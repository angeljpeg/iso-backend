<html lang='es'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Notificación de Estadía</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #059669;
        color: white;
        padding: 20px;
        text-align: center;
        border-radius: 8px 8px 0 0;
      }
      .content {
        background-color: #f0fdf4;
        padding: 30px;
        border-radius: 0 0 8px 8px;
        border: 1px solid #bbf7d0;
      }
      .notification-type {
        background-color: #dcfce7;
        padding: 15px;
        border-radius: 8px;
        margin: 20px 0;
        border-left: 4px solid #059669;
      }
      .details {
        background-color: white;
        padding: 20px;
        border-radius: 8px;
        margin: 20px 0;
        border: 1px solid #e2e8f0;
      }
      .footer {
        text-align: center;
        margin-top: 30px;
        color: #64748b;
        font-size: 14px;
      }
      .urgent {
        background-color: #fef2f2;
        border-left-color: #dc2626;
      }
      .warning {
        background-color: #fffbeb;
        border-left-color: #d97706;
      }
    </style>
  </head>
  <body>
    <div class='header'>
      <h1>📋 Notificación de Estadía</h1>
      <p>Sistema ISO - UTN</p>
    </div>

    <div class='content'>
      <h2>Hola {{nombre}},</h2>

      <div class='notification-type'>
        <h3>🔔 {{tipo}}</h3>
        <p>Has recibido una notificación relacionada con tu estadía.</p>
      </div>

      <div class='details'>
        <h3>Detalles de la notificación:</h3>
        {{#if detalles}}
          {{#each detalles}}
            <p><strong>{{@key}}:</strong> {{this}}</p>
          {{/each}}
        {{else}}
          <p>No hay detalles adicionales disponibles.</p>
        {{/if}}
      </div>

      <p>Por favor, revisa tu panel de estadías en el sistema para más
        información.</p>

      <p>Si tienes alguna pregunta, contacta a tu asesor o coordinador.</p>

      <div class='footer'>
        <p>Fecha: {{fecha}}</p>
        <p>© Sistema ISO - UTN</p>
        <p>Este es un correo automático, por favor no respondas a este mensaje.</p>
      </div>
    </div>
  </body>
</html>